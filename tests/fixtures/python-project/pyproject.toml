[project]
name = "test-python-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115.0", "pydantic>=2.0.0", "uvicorn>=0.34.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "ruff>=0.9.0"]
